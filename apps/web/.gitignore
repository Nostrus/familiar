
# clerk configuration (can include secrets)
/.clerk/

# local env files (contain secrets)
.env.local
.env.*.local

# temporary local sample assets
public/assets/home-samples/

# Next.js generated types (auto-updated, causes whitespace diffs)
next-env.d.ts
